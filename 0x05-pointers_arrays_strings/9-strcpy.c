#include <string.h>

/**
 *_strcpy - copies the string pointer to by a source
 *
 *Description: 'Program's description'
 *
 *@dest: destination
 *@src: source
 *
 *Return: it returns the pointer to dest
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
