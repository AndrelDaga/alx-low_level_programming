#include "main.h"
#include <string.h>

/**
 * _strchr - locates a character in a string
 * @s: var 1
 * @c: var 2
 * Return: returns a pointer
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
