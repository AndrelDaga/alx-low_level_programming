#include "main.h"

/**
 * print_last_digit - prints the last digit of a number
 * @n: the number
 * Return: the last digit which is an integer
 */
int print_last_digit(int n)
{
	int l;

	l = n % 10;
	return (l);
}
