#include "main.h"
#include <stdlib.h>

/**
  * _abs - computes the absolute value of an int
  * @x: the parameter of the function
  *Return: positive int value
  */

int _abs(int x)
{
	return (abs(x));
}
