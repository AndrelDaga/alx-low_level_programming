#include "main.h"
#include "stdlib.h"

/**
 * _abs - computes the absolute value of an integer
 * @int: the integer value to work with
 * Return: the positive integer value
 */
int _abs(int n)
{
	return (abs(n));
}
